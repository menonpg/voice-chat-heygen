[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn voice_chat_heygen_server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/status"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
